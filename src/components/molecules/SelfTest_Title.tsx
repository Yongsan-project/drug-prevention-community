import Text from "../atoms/Text";

const SelfTestTitle = () => {
  const styles = {
    fontSize: "40px",
    fontWeight: "bold",
  };

  return <Text content="마약중독" styles={styles} />;
};

export default SelfTestTitle;
