import Text from "../atoms/Text";

const SelfTestSubTitle = () => {
  const styles = {
    fontSize: "20px",
    fontWeight: "normal",
    color: "purple",
  };

  return <Text content="약물중독(DAST-10)" styles={styles} />;
};

export default SelfTestSubTitle;
